name: Deploy to Alibaba Cloud ECS

on:
  push:
    branches:
      - master
  workflow_dispatch:

env:
  REGISTRY: ghcr.io

# Required for GHCR push permission
permissions:
  contents: read
  packages: write

jobs:
  # ============================================
  # Test
  # ============================================
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Run tests
        run: mvn test -B

  # ============================================
  # Build & Deploy
  # ============================================
  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: success()
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # GHCR enforces lowercase; ${OWNER,,} converts MJB-bot → mjb-bot
      - name: Normalize image name
        run: |
          OWNER="${{ github.repository_owner }}"
          echo "IMAGE_NAME=${OWNER,,}/app-server" >> $GITHUB_ENV

      # ----------------------------------------
      # Login to GitHub Container Registry
      # ----------------------------------------
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # ----------------------------------------
      # Build & push Docker image
      # ----------------------------------------
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # ----------------------------------------
      # Deploy to ECS via SSH
      # ----------------------------------------
      - name: Deploy to ECS
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.ECS_HOST }}
          username: ${{ secrets.ECS_USER }}
          key: ${{ secrets.ECS_SSH_PRIVATE_KEY }}
          script_stop: true
          script: |
            set -e
            IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest"
            echo "=== Pulling ${IMAGE} ==="
            echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
            docker pull "${IMAGE}"

            echo "=== Restarting container ==="
            cd /opt/app
            docker compose down --remove-orphans || true
            docker compose up -d

            echo "=== Waiting for healthy ==="
            sleep 10
            docker ps --filter name=app-server --format 'table {{.Names}}\t{{.Status}}'

            echo "=== Cleaning old images ==="
            docker image prune -af --filter "until=24h"
            docker builder prune -f

            echo "=== Deploy done ==="
